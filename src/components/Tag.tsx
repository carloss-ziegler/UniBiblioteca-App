import { View, Text } from "react-native";
import React from "react";

const Tag = () => {
  return (
    <View className="flex-[0.5] w-60">
      <Text>Tag</Text>
    </View>
  );
};

export default Tag;
